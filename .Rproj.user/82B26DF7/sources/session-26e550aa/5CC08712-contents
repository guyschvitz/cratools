#' getCountryIsoCode: Function to look up ISO3C code for a country
#'
#' @param country.name.rgx character. Country name or regular expression. 
#' @param search.by.iso boolean. Search by iso3c code (TRUE) or by country name (FALSE)? Default: FALSE
#' Example: "Senegal", "^Senegal$", "Seneg", "sene", "enega" all give the same result
getCountryIsoCode <- function(country.name.rgx, search.by.iso = FALSE){
  if(search.by.iso == FALSE){
    countrycode::codelist %>%
      dplyr::select(country.name.en, iso3c) %>%
      filter(grepl(country.name.rgx, country.name.en, ignore.case = T))
  } else {
    countrycode::codelist %>%
      dplyr::select(country.name.en, iso3c) %>%
      filter(grepl(country.name.rgx, iso3c, ignore.case = T))
  }
  
}
