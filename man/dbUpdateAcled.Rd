% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbUpdateAcled.R
\name{dbUpdateAcled}
\alias{dbUpdateAcled}
\title{Update ACLED Data with Upsert Logic}
\usage{
dbUpdateAcled(connection, acled.new.df)
}
\arguments{
\item{connection}{A database connection object created with \code{\link[DBI]{dbConnect}}}

\item{acled.new.df}{A data.frame containing new ACLED data. Must include
columns 'event_id_cnty' and 'timestamp'}
}
\value{
Integer. Number of records successfully processed and upserted
}
\description{
Performs upsert operation on ACLED database table, keeping only the latest
timestamp for each event_id_cnty. This function handles duplicate records
by keeping only the most recent timestamp for each unique event identifier.
}
\examples{
\dontrun{
# Create database connection
con <- DBI::dbConnect(odbc::odbc(), "connection_string")

# Load new ACLED data
acled.new.df <- read.csv("new_acled_data.csv")

# Update database
records.updated <- dbUpdateAcled(
  connection = con, 
  acled.new.df = acled.new.df
)

# Clean up
DBI::dbDisconnect(con)
}
}
