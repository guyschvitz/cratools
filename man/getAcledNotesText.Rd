% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getAcledNotesText.R
\name{getAcledNotesText}
\alias{getAcledNotesText}
\title{Extract conflict event descriptions from ACLED data}
\usage{
getAcledNotesText(
  acled.df,
  ctry.id,
  sdate,
  edate,
  use.regex = TRUE,
  group.by = NULL
)
}
\arguments{
\item{acled.df}{A data.frame or data.table containing ACLED event data}

\item{ctry.id}{Vector of country identifiers (character)}

\item{sdate}{Start date (exclusive)}

\item{edate}{End date (exclusive)}

\item{use.regex}{Logical. Whether to interpret ctry.id as a regex pattern. Default is TRUE}

\item{group.by}{Optional. Time unit to group by: "month", "quarter", or "year". Default is NULL (no grouping)}
}
\value{
A named list of character strings. Each element is a formatted text block for one group.
}
\description{
This function extracts and formats narrative descriptions from ACLED data
for a given country and time period. It can optionally group results by country
and time unit (month, quarter, or year).
}
\examples{
\dontrun{
# Example with sample ACLED data
notes.ls <- getAcledNotesText(
  acled.df = acled.df,
  ctry.id = "Nigeria",
  sdate = as.Date("2025-05-01"),
  edate = as.Date("2025-08-01"),
  group.by = "month"
)
}

}
